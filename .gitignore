# Local tooling / editor state (machine-specific; not for sharing)
.claude/
*.local.json

# Dependencies / build
node_modules/
dist/
build/

# Cloudflare Pages — local secrets & build cache (NEVER commit the API key)
.dev.vars
.wrangler/

# OS / editor cruft
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/
